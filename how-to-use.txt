How to use
----------

Run "npm install" inside this project folder to install all dependencies.

Make sure you use the latest version of the CLI and node.js

Run "ng serve"(http://localhost:4200/) to see the app in action (try "npm start" in case "ng serve" fails).


